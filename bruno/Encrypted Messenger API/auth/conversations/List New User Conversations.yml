info:
  name: List New User Conversations
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/conversations?limit=50"
  params:
    - name: limit
      value: "50"
      type: query
  auth:
    type: bearer
    token: "{{newUserAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
